import { render, screen, cleanup } from '@testing-library/react';

describe("create more than ten devices with the same gateway ip", () => {

    test('asd', () => {
        //const results = createDevice();
        //expect(typeof results).toBe(true);
    });
});